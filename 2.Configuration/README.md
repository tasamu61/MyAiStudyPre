# ２．学習プロセスの構成
１．ではデモプログラムを提示し、実行したがそれを構成する内容について機械学習上で持つ意味、効果などについて説明する。
その詳細やシステム化における現場での応用実例については、本章での説明をもとにしてAIなどを活用し、理解を進めていただきたい。

## 2.1 構成要素
式、モデル	y=a*x+c を使用したが、	
	二次式：y=a*x²+b*x+c	
	三次式：y=a*x³+b*x²+c*x+d	
	や他にも多くの数式が考えられる。
	実際のデータ、目的などによって決まるか、あるいはいくつか試行することが必要かもしれない。
	非常に多様な事柄であり、ここではいくつかの例の説明にとどめる。より詳しくは以下のようなフレーズでAIに問いかけて、納得いくまで問答を続けるこをと勧める。
	「二次式による回帰が実際の業務で使われる例を、ITエンジニア向けに教えて」	
	「一次式では表現できず、二次式なら表現できる現実のデータの例は？」	
	「三次式による回帰を使う実務上の例は？」	
	「一次、二次、三次のモデルから、実際の機械学習ではどうやってモデルを選ぶ？」	
	「モデルを複雑にしすぎると、なぜ過学習が起きる？」	
		
損失関数	(予測値 - 正解値)**2	
	予測値と正解値がどの程度違っているかを数値で表すための関数である。	
	今回採用した二乗誤差（Squared Error） で、回帰では非常に一般的、複数データなら、	
	その平均を取った MSE（平均二乗誤差） がよく使われる。
	いくつかの方法があるが、損失関数をキーワードにすれば多くの情報が得られる。	

初期値	a=0, b=0	
	学習を始める前に、パラメータ a, b にどんな値を入れておくかという設定である。
	今回のように「一次式＋二乗誤差」の組み合わせは、グラフにするとお椀を伏せたような形（凸関数）になっており、
	どこから転がし始めても、最後は同じ底（最適解）にたどり着く。そのため初期値を 0 にしても問題は起きない。
	ただし式が複雑になったり、後述のように損失関数の形が単純な谷ではなくなったりすると、
	初期値によって「どの谷に落ち着くか」が変わってしまうことがある（局所解）。
	この点は、下記「谷がひとつじゃない場合」で実際に確認できる。

学習率	0.01	
	１回の学習で、勾配（傾き）をもとにパラメータをどれだけ動かすかを決める係数である。
	大きすぎると値が行き過ぎて発散し、Lossがどんどん大きくなってしまう。
	小さすぎると一歩の動きが小さすぎて、なかなか最適解にたどり着かない。
	また式の次数が上がる（二次、三次になる）ほど勾配自体の値が大きくなりやすいため、
	同じ学習率でも次数が高い式では発散しやすくなる。次数に応じて学習率の目安を小さくする必要がある。

epoch（回数）	100	
	学習（パラメータの補正）を何回繰り返すかという設定である。
	回数を増やせば増やすほどLossが下がり続けるわけではなく、ある程度で下げ止まる。
	下げ止まった後にepochだけ増やしても、結果はほとんど変わらない。
	何回で下げ止まるかは、データのばらつきや学習率、式の次数によって変わってくる。

## 2.2 サンプルプログラム

本章では、内容が重複するデモを多数順番に追うのではなく、次の2本を中心に確認する。

### 2.2.1 モデル・損失関数・係数の違いを見る

[02-1Loss_explorer.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-1Loss_explorer.html)

データの形と、あてはめる式の次数を独立に選び、一次・二次・三次のモデルの違いを確認できる。
またMSE、MAE、Huberなど損失関数を変えた場合や、外れ値を含めた場合に、Lossや係数がどのように変化するかを見ることができる。

第1章では `y = a*x + b` というひとつのモデルを学習したが、実際には「どのモデルを使うか」「どの損失関数を使うか」も人が決める必要があることを、このデモで確認する。

### 2.2.2 初期値によって結果が変わる例を見る

[02-x-1.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-x-1.html)

第1章のような単純な問題では、初期値を0にしても同じ最適解へ向かいやすい。
一方、Lossの谷が複数ある場合には、初期値によって到達する場所が変わることがある。
このデモでは、その「局所解」と初期値の関係を視覚的に確認する。

## 2.3 その他の参考デモ

以下は、上の2本で扱った内容を別の見せ方で確認するための参考デモである。
すべてを順番に実行する必要はなく、興味のあるものを参照されたい。

- [02-2SomFunctions.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-2SomFunctions.html)
- [02-3ml_learning_demo.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-3ml_learning_demo.html)
- [02-4Mlearn.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-4Mlearn.html)
- [02-5RegressionLab.html](https://tasamu61.github.io/MyAiStudyPre/2.Configuration/02-5RegressionLab.html)

02-5RegressionLab.htmlは、モデル、損失関数、初期値、学習率、epochなどをまとめて変更できる総合的なデモである。
02-1Loss_explorer.htmlと扱う内容に重なる部分が多いため、本章では主サンプルではなく参考として位置づけている。
